<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Login POS · {{ $brandName }}</title><link rel="icon" href="{{ $brandFaviconDataUri ?: asset('favicon.ico') }}">@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="portal-auth-page pos-auth-theme"><main class="portal-auth-shell">
    <section class="portal-auth-brand"><a class="brand brand-light" href="{{ route('pos.login') }}"><span class="brand-mark {{ $brandLogoDataUri ? 'has-image' : '' }}">@if($brandLogoDataUri)<img src="{{ $brandLogoDataUri }}" alt="">@else{{ strtoupper(mb_substr($brandName,0,1)) }}@endif</span><span><strong>{{ $brandName }} POS</strong><small>Terminal Penjualan</small></span></a><div><p class="eyebrow">POINT OF SALE</p><h1>Kasir cepat.<br>Stok tetap akurat.</h1><p>Portal khusus kasir untuk transaksi penjualan.</p></div><a href="{{ route('login') }}">Masuk ke Back Office →</a></section>
    <section class="portal-auth-form"><form method="POST" action="{{ route('pos.login.store') }}" class="login-form">@csrf<p class="eyebrow">POS ACCESS</p><h2>Login kasir</h2><p class="muted">Gunakan akun dengan role POS.</p><label>Email</label><input type="email" name="email" value="{{ old('email','kasir@labelin.test') }}" required autofocus>@error('email')<small class="field-error">{{ $message }}</small>@enderror<label>Password</label><input type="password" name="password" value="password" required><label class="check-row"><input type="checkbox" name="remember"> Ingat perangkat ini</label><button class="button button-primary button-wide">Masuk terminal POS →</button><a class="auth-help-link" href="{{ route('password.request',['portal'=>'pos']) }}">Lupa password?</a></form></section>
</main></body></html>
