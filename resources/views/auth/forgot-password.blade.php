<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Reset Password · {{ $brandName }}</title><link rel="icon" href="{{ $brandFaviconDataUri ?: asset('favicon.ico') }}">@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="simple-auth-page"><form method="POST" action="{{ route('password.email') }}" class="login-form simple-auth-card">@csrf<a class="brand auth-card-brand" href="{{ $portal === 'pos' ? route('pos.login') : route('login') }}"><span class="brand-mark">{{ strtoupper(mb_substr($brandName,0,1)) }}</span><span><strong>{{ $brandName }}</strong><small>{{ $portal === 'pos' ? 'POS' : 'Back Office' }}</small></span></a><p class="eyebrow">PASSWORD RECOVERY</p><h2>Reset password</h2><p class="muted">Masukkan email. Link reset akan dikirim melalui mailer aplikasi.</p>@if(session('status'))<div class="toast-success">{{ session('status') }}</div>@endif<label>Email</label><input type="email" name="email" value="{{ old('email') }}" required autofocus>@error('email')<small class="field-error">{{ $message }}</small>@enderror<button class="button button-primary button-wide">Kirim link reset</button><a class="auth-help-link" href="{{ $portal === 'pos' ? route('pos.login') : route('login') }}">← Kembali ke login</a></form></body></html>
